{
  "name": "easyslide-desktop",
  "private": true,
  "description": "EasySlide desktop wrapper",
  "author": "Anionex",
  "version": "0.3.0",
  "main": "main.js",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "electron .",
    "dist:win": "electron-builder --config electron-builder.yml --win",
    "dist:mac": "electron-builder --config electron-builder.yml --mac"
  },
  "devDependencies": {
    "electron": "^43.1.1",
    "electron-builder": "^26.15.3"
  },
  "dependencies": {
    "hyperframes": "0.7.71"
  }
}
